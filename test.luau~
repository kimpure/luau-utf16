local utf16 = require('./init');
print(utf16.codepoint('A'));

local encodeString = utf16.encode('A');
print(encodeString);
local decodeString = utf16.decode(encodeString);
print(decodeString);












